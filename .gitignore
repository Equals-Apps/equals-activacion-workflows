# Shared skills library — not part of this app, kept local per project decision
.agents/skills/

# Secrets — never commit env vars (N8N_WEBHOOK_SECRET, NEXTAUTH_SECRET, Google OAuth creds, etc.)
.env
.env.local
.env.*.local

# Node / Next.js (repo has no app code yet, but avoid a second commit fixing this later)
node_modules/
.next/
out/
.vercel/
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
